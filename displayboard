#include "headers.h"

void displayBoard(int board[8][7])
{
    std::cout<<"Current board: "<<std::endl;
    for (int y = 0; y < 7; y++)
    {
        for(int x = 0; x < 8; x++)
        {
            std::cout<<board[x][y]<<"\t";
        }
        std::cout<<std::endl;
    }
}
